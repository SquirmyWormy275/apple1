# Artwork subsystem

`portal/` contains original project-native machine frames. `provenance/`
contains one JSON record per published asset family. Runtime state visuals are
generated by `neural1.visualization`; host previews belong under ignored
`out/`. External reference material is never copied here without established
rights. Design rules and the heritage source record are in `docs/visual-system/`.
