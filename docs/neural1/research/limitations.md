# Limitations

The world is not a full cycle-accurate Apple-1 emulator; modeled WozMon run
requests do not execute arbitrary code. Provider integrations and final Pi
benchmarks are uncommissioned. The experiment prototypes establish interfaces,
not emergent-culture findings. Historical component data is absent by design
until sources are curated. ASCII previews are not verified on the physical
display. No physical launch mechanism is claimed.
