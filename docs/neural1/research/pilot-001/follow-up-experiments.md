# Follow-up experiments

1. Replay every completed cell and verify exact model-record consumption.
2. Fork candidate anomalies at their preceding checkpoint and remove one named factor.
3. Replicate candidate differences across new seeds before model-family claims.
4. Test any candidate invariant with `BREAK` against held-out cells.
5. Add stronger 3B–4B models through registry records without changing experiment definitions.

## Priority after Pilot 001

1. Pre-register a thermal ceiling and use a cooled/power-bounded host before
   resuming the 39-cell remainder.
2. Run a small, separate prompt-treatment study (zero-shot versus one explicit
   valid WozMon example) with the same strict parser; do not rewrite Pilot 001.
3. Interleave model/experiment blocks so an operational stop does not erase
   entire experiment families.
4. Test exact transcript replay for every new completed cell and compare final
   snapshot hashes after controlled interruption.
