# Anomalies and candidate discoveries

These detector outputs are investigation targets, not facts.

No automated candidate discovery passed the configured detector. Human review
identified one follow-up target: all 156 responses failed the strict WozMon
grammar despite different families and seeds. This is an **observed pilot
failure pattern**, not an invariant or cross-model replication claim because
TinyLlama was incomplete and only one experiment prompt was exercised.

```json
[]
```
