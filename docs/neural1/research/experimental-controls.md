# Experimental controls

Matched runs hold runtime version, model record, seed relationship, initial
snapshot, scheduling, task sequence, memory budget, and scoring version fixed
unless one is the named factor. Context destruction, catastrophes, migration,
and blinded fields are explicit events. Reviews record what was hidden and when
it was revealed. Stop rules are declared before outcome inspection.
