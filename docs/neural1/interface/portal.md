# Portal

The checked 40-column prototype is in `art/portal/apple1-portal.txt`. The
virtual state machine implements COMPUTER, FIELD LIBRARY, NEURAL1, ABOUT, and
return. It does not claim a physical launcher. A future CFFA1 adapter must be
separately verified and replace the launch interface without changing mode
semantics. Any transition animation must accept one-key skip.
