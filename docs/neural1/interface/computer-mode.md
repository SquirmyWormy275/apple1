# COMPUTER mode

COMPUTER means ordinary Apple-1/Replica use. It performs no model call,
interpretation, recommendation, experiment scheduling, or NEURAL1 persistence.
The Pi is passive/unneeded. Future physical entry should return to WozMon or the
ordinary software environment, while Reset remains the unconditional recovery
path. Current output `[V] COMPUTER / MODELED WOZMON` is virtual only.
