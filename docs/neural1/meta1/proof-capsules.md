# Proof capsules

A capsule binds a claim/scope to dataset hash, runs, evidence/artifact hashes,
analysis version, effect size, uncertainty, counterexamples, causal tests, and
reproduction command. Its ID is content-derived excluding generation time.
Capsules are portable indexes, not substitutes for raw evidence. Public claims
should resolve through capsule, runs, artifacts, and raw records.
