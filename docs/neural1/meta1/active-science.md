# Active science and discovery

The research queue publishes every priority component: uncertainty, novelty,
expected information gain, cross-experiment relevance, and normalized compute
cost. Discovery scans for convergence, transitions, extinction clusters,
repeated structures, regressions, and anomalies; each output is a candidate
discovery with an ID, detector version, dataset hash, and false-positive review,
not an established fact.
