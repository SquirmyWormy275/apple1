# Repository review — September 16, 2026

## Purpose and scope

Refresh the human-facing entry pages and reconcile the recent Pi work with the
repository's published state. This is a documentation and integration-visibility
review, not a new commissioning session, full implementation audit, or licensing
clearance of the collection. A separate, formatting-only test correction was
made when fresh Pi-branch CI exposed it; that follow-up is recorded below.

The review read the repository through GitHub, including its branch inventory,
open and historical pull requests, main-tree inventory, entry documentation,
architecture rules, validator, CI workflow, troubleshooting history, native Pi
closeout, and Field Library index. A local clone could not be obtained, so no
local fresh-checkout suite or live-device test is claimed. The small test-spacing
correction was checked locally for blob identity and unchanged Python syntax tree.

## Inspected revisions

| Ref or role | Revision at initial inspection |
|---|---|
| `main` | `2c40f1c5c63a083184eb724df4500cd1427bd3d2` |
| Main source tree | `2c16ffb1d3c0a311107e85930bbe542082239706` |
| `neural1/pi-completion-r3` | `32102a1ad46210c4792680293d136cf0719f8fe7` |
| `work/propeller-overnight-preparation-2026-09-11` | `6d644d26425e8f62e48b9627b1c56430be144cbc` |
| Frozen research archive | `archive/neural1-1976-research-2026-08-30` at `f1ac1958c2e8906510fc963422ed1801f0231b1b` |

The remote inventory contained 17 branches before this review's documentation
branch was created. Every inventoried branch and archive history was retained.
Only the new documentation branch and the subsequent fast-forward test-formatting
commit on the Pi branch were written. Main, the Propeller branch, tags, archives,
and deployed files were not changed. Unpushed local worktrees were not inventoried.

## Findings and changes

### Pi completion was preserved but not integrated

The Pi branch was initially 40 commits ahead of `main`, zero behind, with 59
changed files. The inspected pull-request history contained no Pi-completion PR.
[Draft PR #7](https://github.com/SquirmyWormy275/apple1/pull/7) now exposes that
work for review without merging it.

The [pinned Pi closeout](https://github.com/SquirmyWormy275/apple1/blob/32102a1ad46210c4792680293d136cf0719f8fe7/docs/neural1/operations/pi-closeout-2026-09-10.md)
records the installed application's different revision, late-SSD startup wiring,
normal operator entry point, final power test, acceptance outcomes, and sanitized
evidence. The refreshed pages distinguish a commissioned Pi from both `main`
and physical Replica qualification.

### Entry pages mixed navigation with historical status

The root README, project map, and NEURAL1 overview now separate current operation,
source-branch state, and hardware readiness. [Current status](../STATUS.md) is
the shared status summary; this directory retains the review detail.

All five experiments are individually linked. Earlier reports, image/SSD
commissioning guides, and audits remain accessible with their historical role
made clear. The root development commands now include `tests/test_storage.py`
and the same off-device/demo-cleanliness assertions as the existing CI workflow.
The workflow itself is unchanged.

The existing Field Library index says 41 lesson packets while the Pi closeout
reports 42 including SP01. This count discrepancy is explicitly retained as a
catalog/index reconciliation item, not treated as proof of a missing lesson on
main or used to close the outstanding corpus-review gate.

### Propeller preparation is not physical qualification

[PR #6](https://github.com/SquirmyWormy275/apple1/pull/6) remains independent and
unmerged. A current-position note was added above the troubleshooting history.
The final PR patch shows only that insertion: the August 27 and September 2
sections are unchanged. No new measurements, serial activity, board diagnosis,
or hardware repair are claimed.

### Evidence and rights remain intact

Historical Pilot 001 is not overwritten by later native acceptance. The staged
historical index is not promoted merely because the Pi branch contains a reviewed
console corpus. Collection provenance and artwork rights remain qualified by
their existing records. No photos, weights, raw disk images, credentials, private
machine configuration, or new third-party material were added.

## Changed surface

[Documentation PR #8](https://github.com/SquirmyWormy275/apple1/pull/8) is limited
to eight Markdown files: the root README, project map, NEURAL1 overview,
troubleshooting preface, repository index, repository-held wiki home, this review,
and the new current-status page. That PR changes no runtime, tests, schemas,
configuration, CI, firmware, capture bytes, preservation artifacts, or scientific
evidence files. No existing path is deleted or moved.

The wiki edit updates `wiki/Home.md` in this repository. It does not publish the
separate live GitHub Wiki.

## CI follow-up on the Pi branch

Opening PR #7 triggered fresh validation on its preserved head. The
[initial run](https://github.com/SquirmyWormy275/apple1/actions/runs/35183729599)
failed Ruff I001 in `tests/test_neural1_field_lesson_catalog.py`; later checks
were skipped, so that run was not reported as a successful validation.

Commit `63012aca3eadeac6ab401399cd51e2c99fe840f2` removes exactly one extra newline
between the import block and `ROOT`. The original source blob was independently
matched to `3e62b48acf6f62caf7989afcff280c514f3a6e62`; parsed Python syntax trees
before and after the correction were identical. No imports, assertions, runtime
modules, provider settings, or deployed files changed. The previous 40 commits
remain ancestors; no force-push or history rewrite occurred.

The resulting [Pi validation run](https://github.com/SquirmyWormy275/apple1/actions/runs/35183942893)
passed installation, Ruff, mypy, pytest, repository validation, deterministic
demo, the explicit off-device assertion, and tracked-tree cleanliness. The Pi
branch is now 41 commits ahead of the inspected main baseline, with the same
59 changed files. PR #7 remains draft because a full implementation review is
not replaced by passing software checks.

## Documentation validation and integration

[Validation run 35183884230](https://github.com/SquirmyWormy275/apple1/actions/runs/35183884230)
passed on the initial documentation head
`3439c825bcb932bfaab6b8e52d096deeeddbe049`. The full workflow includes installation,
Ruff, mypy, pytest, repository/schema/link/art/provenance validation,
deterministic demo, explicit `serial_opened=false`, and a clean tracked tree.
This follow-up updates the status/review text; its final acceptance is the run
attached to the resulting PR head, not an assumption that the earlier run
covers later edits.

The final changed-file list must remain documentation-only. New relative links
must resolve, and branch-only Pi records use pinned GitHub links rather than
nonexistent paths on `main`.

Merge requires separate authorization. When the Pi work is integrated, reconcile
its README addition with these entry pages and update the status summary. Do not
rerun installation or hardware tests solely to publish already-recorded work.
