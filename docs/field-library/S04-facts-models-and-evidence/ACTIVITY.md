# S04 Activity: sort the statements

**Status:** OFF-DEVICE. Paper only. Nothing is powered on or connected.

Use `assets/sorting-card.txt` throughout. Mark each statement **F** (fact
someone observed), **M** (model or specification), or **C** (claim needing
evidence), and give one line of reasoning.

## Part A: the three (this is the first result)

| # | Statement | F/M/C | Reason |
|---|---|---|---|
| a | The Woz Monitor occupies 256 bytes from `$FF00` to `$FFFF`. | | |
| b | This board's EEPROM contains the `110REV03` firmware. | | |
| c | Opening the FT232R from the host garbled the display. | | |

## Part B: ten more

| # | Statement | F/M/C |
|---|---|---|
| 1 | The Apple-1 retailed for $666.66. | |
| 2 | This machine's serial port works. | |
| 3 | The manual says the replica runs at 9600 baud. | |
| 4 | `line-input-0300.hex` returned to the Monitor in the repository emulator. | |
| 5 | `line-input-0300.hex` will return to the Monitor on this board. | |
| 6 | The keyboard character register is at `$D010`. | |
| 7 | The Pi reported the device node `/dev/ttyUSB0` on 2026-08-27. | |
| 8 | The USB serial value `00000000` uniquely identifies this cable. | |
| 9 | Two hundred Apple-1 boards were made. | |
| 10 | This replica is worth what an original Apple-1 is worth. | |

## Part C: the same sentence, three ways

Rewrite this sentence three times, once so it belongs in each bin. You may add
or remove detail, but you may not state anything untrue.

> "The display works."

## Part D: find the smuggled model

Each pair below states a fact and a model as if they were one thing. Underline
the model half.

1. "The board powered on and showed the garbage screen, which means the video
   circuitry is healthy."
2. "The source archive hashes correctly, so the firmware is intact."
3. "I pressed RESET and got a backslash, so the Monitor ROM is the stock one."

## Part E (optional, STUDY level): the evidence card

Write a five-line evidence card for one claim of your choosing about this
project, using these fields: claim, current bin, what exists now, what would
move it one bin, who would have to do that. Compare with the sample in
`ANSWERS.md`.

## What this activity does not do

It sorts statements on paper. It resolves none of the open questions it uses as
examples, and it authorizes no hardware action.
