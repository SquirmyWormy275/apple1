# B01 Activity: match the job

**Status:** OFF-DEVICE. Paper only. No program in this packet is run anywhere.

## Part A: five tasks (this is the first result)

| # | Task | Language | Reason |
|---|---|---|---|
| 1 | Add up twenty numbers a person types in | | |
| 2 | Read the keyboard register directly and react to one bit | | |
| 3 | Print a multiplication table | | |
| 4 | Fit a program into 26 bytes | | |
| 5 | Try five different versions of a formula in an afternoon | | |

## Part B: what BASIC hides

For each BASIC line, list the bookkeeping assembly would make you do.

| BASIC | Hidden work |
|---|---|
| `10 PRINT "HI"` | |
| `20 A = B + C` | |
| `30 INPUT "NAME", N$` | |
| `40 GOTO 10` | |

## Part C: Apple-1 BASIC facts

True or false. All are about Apple-1 BASIC specifically.

| # | Statement | T/F |
|---|---|---|
| 1 | It supports decimals like 3.5. | |
| 2 | `PRINT 38 MOD 9` gives 2. | |
| 3 | A string variable can be called `NAME$`. | |
| 4 | Strings must be dimensioned before use. | |
| 5 | You can suppress the question mark that `INPUT` prints. | |
| 6 | Arithmetic works from the command line without a program. | |

## Part D: cost and benefit

Fill both columns honestly. Do not put an empty cell in either.

| | Assembly | BASIC |
|---|---|---|
| Speed of writing | | |
| Speed of running | | |
| Memory used | | |
| Control over details | | |
| What must already be present | | |
| Ease of changing later | | |

## Part E: the unfair comparison

Someone says: "Assembly is better, look, the assembly version of `PRINT` is only
a few instructions and BASIC needs a whole interpreter in ROM."

Find the error in that comparison.

## Part F (optional): the third option

The Replica 1 Plus ROM also holds an assembler, Krusader, reached by typing
`F000R`. Where does an on-machine assembler sit between hand-entered bytes and
BASIC? Name one thing it improves over hand entry and one thing it does not
change.

## What this activity does not do

It compares languages on paper. It runs nothing, and it authorizes no hardware
action.
